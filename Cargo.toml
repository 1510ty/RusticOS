# /Cargo.toml
[workspace]
members = [
    "kernel",
    "common",
    "user/libuser",
    "user/apps/calc",
]
resolver = "2"

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
lto = true
opt-level = 3